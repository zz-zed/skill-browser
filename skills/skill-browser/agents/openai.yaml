interface:
  display_name: "Skill 导航器"
  short_description: "发现、解释、比较并推荐本机已安装的 Agent Skills"
  default_prompt: "Use $skill-browser to show the installed Skill capability map."

policy:
  allow_implicit_invocation: false
